# Failed Investigations: data-transform Subsystem

Condensed failure summaries for investigations targeting the data-transform subsystem. Last updated 2026-04-12 (condensed 059–078).

## Summary Table

| File | Hypothesis | Why Failed | Stage | Key Lesson |
|------|-----------|------------|-------|------------|
| 001.md | Token transfer operation IDs are off by one | `EventMeta.OperationIndex` is already 1-indexed; passing it to `toid.New()` produces the correct TOID with no adjustment needed | hypothesis | Different transform functions use different indexing adjustments depending on their upstream abstraction; verify the source index origin before assuming an off-by-one |
| 002.md | `transaction_count` should include failed transactions | Tests and golden outputs consistently encode `transaction_count` as the successful-transaction count, reflecting intentional historical BigQuery semantics | hypothesis | Some ledger columns carry legacy BigQuery conventions that differ from their name; check golden outputs before proposing schema changes |
| 003.md | Ledger bucket-list byte size mirrors live Soroban state incorrectly | The Stellar protocol exposes only a single `totalByteSizeOfLiveSorobanState` field; no separate bucket-list-size measurement exists in the XDR | reviewer | Verify the XDR struct actually defines the hypothesized field before claiming a dual-path mix-up |
| 004.md | Config-setting bucket-list window fields are fabricated from live Soroban state | Protocol defines only live-Soroban-state window fields (`StateArchivalSettings`); there is no separate bucket-list-window XDR field | reviewer | Check the XDR definition first; the alleged source field may simply not exist in the protocol |
| 005.md | `soroban_resources_read_bytes` silently duplicates disk read bytes | `SorobanResources` XDR has never defined a generic `ReadBytes` field; `DiskReadBytes` is the only read-bytes metric | reviewer | Check the XDR struct definition before hypothesizing that one field is wrongly substituted for another |
| 006.md | Fee-bump `account` field is overwritten by the fee sponsor | `Envelope.SourceAccount()` already returns the inner TX source for fee-bump envelopes; `account` and `fee_account` are correctly distinct | hypothesis | Verify SDK helper semantics before assuming that direct XDR field access would differ; fee-bump disambiguation is handled by the SDK |
| 007.md | Normalized offer IDs collide after 6-decimal `%f` formatting | Exact duplicate of already-published finding `data-integrity/001-dim-offer-id-collision` (same root cause, same code path, same impact) | reviewer | Cross-check `ai-summary/success/` published files before investigating; this finding was already confirmed |
| 008-010.md | `tx_signers` encodes signatures as fake account IDs (three separate submissions) | All three are duplicates of the already-published finding `001-tx-signers-encode-signature-bytes`; files 008 and 009 each re-submitted the same rejected hypothesis; 010 was the third repetition | reviewer | Check published success files and existing fail entries before re-submitting; 008, 009, and 010 all cover the identical mechanism |
| 011.md | Account sequence extension fields lose nullability in Parquet | `zero.Int` serializes invalid values as `0` in JSON already, so the nullability distinction is destroyed before the Parquet converter runs | hypothesis | Not every schema wrapper is semantically nullable; audit the JSON output first before tracing a missing-null through Parquet |
| 012.md | Operation Parquet omits `details_json` | `TransformOperation()` writes the same map to both `OperationDetails` and `OperationDetailsJSON`; Parquet preserves it via the `details` column | hypothesis | Confirm a "missing" Parquet field is genuinely absent, not just serialized under a different alias |
| 013.md | Custom token asset fields should be backfilled from `contract_id` | The upstream token-transfer model intentionally omits `Asset` for custom tokens and provides `contract_address` instead; `contract_id` is already exported | hypothesis | Upstream model omissions for custom tokens are by design; `contract_id` is the correct identifier and the gap is not a transform bug |
| 014.md | `restored_key.ledger_key_hash` exports full XDR instead of its hash | PoC proved a format inconsistency, but repo unit tests and golden outputs deliberately assert base64 for this table; no codebase contract requires the hex-digest representation here | final-review | A PoC showing format inequality is not sufficient; the target field must have an explicit schema contract requiring the alternative format before the finding qualifies as data corruption |
| 015+017.md | `evicted_ledger_keys_hash` exports base64 XDR instead of hex SHA-256 (two submissions) | PoC proved format mismatch; `internal/transform/ledger_test.go` explicitly asserts the base64 form; no codebase contract requires hex-digest for this ledger field | final-review | Repository regression tests are authoritative evidence of design intent; a naming/format inconsistency across tables is not confirmed corruption without a stated codebase contract |
| 016.md | Trade Parquet collapses null `rounding_slippage` into explicit `0` | `trade_type` already distinguishes order-book from LP trades on every row; null vs. zero `rounding_slippage` is recoverable from the same Parquet row via `trade_type` | final-review | A single-column null-vs-zero collision is not unrecoverable data corruption if a sibling discriminator column makes the semantic distinction recoverable |
| 018.md | Deleted signer rows use the prior `last_modified_ledger` by design | Code and tests confirm the intentional semantic: `last_modified_ledger` on synthetic deletion rows records the last ledger where the signer existed, not the deletion ledger | hypothesis | Synthetic deletion rows in this package use a deliberate "last-seen" semantic for `last_modified_ledger`; this is not stale state |
| 019.md | `LedgerTransaction.tx_result` includes the full result pair intentionally | `TransactionOutput.TxResult` and `LedgerTransaction.tx_result` represent two different output contracts; tests explicitly anchor the latter to `TransactionResultPair` | hypothesis | Different output structs can have different contracts for identically named fields; confirm the target struct's spec before claiming corruption |
| 020.md | Contract-data balance rows drop account-backed token holders | Account-backed SAC balances live in classic trustlines and native account fields, not in Soroban `ContractData`; the SAC protocol does not store classic holdings in contract storage | reviewer | Understand the SAC storage model before hypothesizing missing rows; classic trustline holders are not in ContractData |
| 021.md | (same as 020) Contract-data balance rows drop account-backed token holders | Exact duplicate of fail entry 020; `GetContractId()` is the correct filter because SAC only stores contract-owned balances in `ContractData` | reviewer | Cross-check existing fail entries before re-submitting; the SAC storage model was already established in 020 |
| 022.md | Config-setting Parquet live-state window aliases bucket-list window | The `LiveSorobanStateSizeWindow: BucketListSizeWindowInt` typo is dead code because `TransformConfigSetting()` already duplicates the same source slice into both fields; the Parquet aliasing produces no observable difference today | hypothesis | A converter typo is only a live data-integrity bug if the pre-conversion fields can actually diverge; check the JSON-layer transform and fixtures before treating a bad Parquet assignment as observable corruption |
| 023.md | `min_seq_age` / `min_seq_ledger_gap` should stay nullable for absent V2 preconditions | `MinSeqAge` and `MinSeqLedgerGap` are mandatory inline scalars in `PreconditionsV2`, not optional members; the XDR wire format cannot encode a distinct null state, so zero is the correct exported value | hypothesis | For transaction preconditions, only fields backed by optional XDR members (like `MinSeqNum`) can support true null-vs-zero hypotheses; verify the XDR member is optional before treating zero export as lost-nullability |
| 024a.md | Config-setting export zeroes `EvictionIterator` rows (config_setting_id=13) | Exact duplicate of published finding `data-integrity/006-eviction-iterator-empty-shell`; same root cause, same affected schema, same impact | reviewer | Cross-check `ai-summary/success/` across all subsystems, not just the target subsystem; the same underlying bug may have been published under a different subsystem classification |
| 024b.md | Contract-data Parquet drops `ledger_key_hash_base_64` | PoC confirmed the field is absent from `ContractDataOutputParquet`, but the export pipeline already crashes at `writer.NewParquetWriter()` due to MAP-type tag failures before `ToParquet()` is ever reached; the omission is latent behind a larger initialization failure and thus out of scope as a current data-integrity bug | final-review | A PoC demonstrating struct schema drift is insufficient if the corrupt conversion path is unreachable in production due to a prior failure (`writer.NewParquetWriter()` aborts before any `record.ToParquet()` call for contract-data rows) |
| 025.md | Config-setting export zeroes `ContractParallelCompute` rows (config_setting_id=14) | Exact duplicate of published finding `data-integrity/005-config-parallel-compute-empty-shell`; same root cause, mechanism, and impact | reviewer | Cross-check `ai-summary/success/` across all subsystems before submitting |
| 026.md | Config-setting export zeroes `ContractScpTiming` rows (config_setting_id=16) | Exact duplicate of published finding `export-pipeline/006-config-scp-timing-exports-empty-shell`; same root cause, mechanism, and impact | reviewer | Cross-check `ai-summary/success/` across all subsystems before submitting |
| 027.md | Config-setting export drops `tx_max_footprint_entries` from ledger-cost-ext rows (config_setting_id=15) | Exact duplicate of published finding `export-pipeline/005-config-ledger-cost-ext-drops-footprint-cap`; same root cause, mechanism, and impact | reviewer | Cross-check `ai-summary/success/` across all subsystems before submitting |
| 028.md | Token-transfer export rounds large raw amounts before scaling | Exact duplicate of published finding `data-integrity/002-token-transfer-float64-rounding`; same `strconv.ParseFloat` → `* 0.0000001` mechanism | reviewer | Cross-check `ai-summary/success/` across all subsystems before submitting; findings may be published under a different subsystem than the one being investigated |
| 029.md | Contract-data balance parsing depends on fixed `ScMap` entry order | Soroban `SCMap` values are canonically ordered by key before serialization; the specific key names used (`amount` < `authorized` < `clawback`, `asset_code` < `issuer`) sort into the exact order the positional parser expects, so the brittle indexing is unreachable for legitimate on-chain rows | hypothesis | Order-sensitive `ScMap` parsing is only a live bug if the protocol allows non-canonical entry ordering; verify host/protocol ordering guarantees before treating positional map access as an actual data-loss path |
| 030+031.md | Failed path-payment rows emit zero outcome amounts; failed LP deposit/withdraw rows emit native reserve assets | Both patterns exactly match the canonical Horizon SDK upstream implementation (`stellar/go processors/operation/`), which uses the same zero-default pattern and the same misleading "omitted" comment; this is the ecosystem-standard convention for failed operations, not ETL fabrication | reviewer | Before claiming an ETL emits fabricated values, check the canonical upstream SDK; if the upstream uses the same zero-default pattern for failed operations, the ETL is replicating the ecosystem convention |
| 032.md | (same as 030) Failed path-payment rows fabricate zero outcome amounts | Exact duplicate of 030; same code path, same conclusion, same lesson | reviewer | Check existing fail entries for identical titles and mechanisms before re-submitting |
| 033.md | Fee-bump `max_fee` field truncates the outer fee-bump bid (uint32 narrowing) | `max_fee` intentionally carries the inner transaction fee via `Envelope.Fee()`; the outer fee-bump bid is separately stored in `new_max_fee` (int64); the uint32 mismatch does not touch the outer fee path | hypothesis | In fee-bump transactions, similar fee field names refer to different envelope layers; confirm which helper (`Fee()` vs `FeeBumpFee()`) feeds each exported column and cross-check existing transaction tests for the intended split |
| 034-040.md | Float64 precision loss for multiple entity transforms: account balance/liabilities, trade claimed amounts, trustline balance/liabilities, path-payment settlement amounts, change-trust limit, offer operation amounts [039], LP withdraw reserve and share amounts [040] | All affected code paths use `ConvertStroopValueToReal()` with `big.NewRat().Float64()` — the best possible float64 conversion and the project's established correct path; the precision loss is an inherent float64 schema design limitation, not a fixable coding error; unlike VIABLE findings 016 (inferior inline conversion) and 017 (exact string discarded via ParseFloat), no better conversion path exists but is unused here; `transactionOperationWrapper.Details()` cited as evidence in 037/038/039/040 is dead code | reviewer | Distinguish (a) conversion bugs where a better path exists but is bypassed → VIABLE; (b) code that computes an exact value then discards it → VIABLE; (c) code that goes directly from XDR Int64 to float64 via `big.NewRat().Float64()` → NOT_VIABLE schema design limit; dead code paths cannot serve as evidence of "a better path available but not used" |

| 041.md | `extra_signers` parquet list tag is invalid | `type=MAP, convertedtype=LIST` exactly matches the parquet-go library's own documented tag for `[]string` repeated columns; the tag is library convention, not a local bug | hypothesis | Before treating a parquet tag shape as malformed, compare it against the library's own examples; parquet-go tag grammar is idiosyncratic and its physical-type tokens do not have to match the logical converted type |
| 042.md | Duplicate `convertedtype=` parquet tags corrupt integer columns | `parquet-go`'s `StringToTag()` overwrites the stored field on each `convertedtype=` occurrence; the later `UINT_64` value wins deterministically, producing stable schema rather than a mixed parse | hypothesis | When a suspected schema bug depends on parser ambiguity, inspect the actual parser; a messy tag string is not a live integrity defect if the downstream parser resolves it deterministically |
| 043.md | Ignored `shouldExport` flag leaks nonce contract-data rows | The ignored bool is redundant: nonce rows return an empty `ContractId`, and the caller's existing empty-`ContractId` guard already suppresses those rows before append | hypothesis | An ignored return value is only actionable if no other guard preserves the same contract; always trace the complete call site before turning a discarded bool into a data-loss claim |
| 044.md | Missing account sequence-extension fields should stay null, not `0` | Golden outputs explicitly assert `"sequence_ledger":0` and `"sequence_time":0`; zero is the repository's established export contract for absent V3 extension fields | hypothesis | Golden outputs are decisive for output-contract questions; if a suspicious representation is already asserted in checked-in fixtures, treat it as design intent |
| 045+046.md | `create_contract` omits derived contract ID / `upload_wasm` leaves `contract_code_hash` empty | Soroban requires exhaustive footprint declaration — the newly created `ContractInstance` / `ContractCode` entry MUST appear in `ReadWrite`; the footprint-scanning helpers therefore always find a valid ID/hash in production; test fixtures use generic envelopes with no `SorobanData` | reviewer | Test fixtures with empty Soroban footprints do not reflect production transaction structure; verify whether the target entry type is present in production footprints before claiming the footprint scanner returns empty results |
| 047.md | `restore_footprint` mislabels SAC balance restores as the asset contract | `ContractData.Contract` identifies the storage-owning contract (the asset contract) by Soroban design; the holder is an internal key component; both stellar-etl and upstream Horizon use identical extraction (`contractData.Contract.GetContractId()`) | reviewer | `ContractData.Contract` is the storage namespace owner by protocol design, not any entity referenced within the key payload; always check the canonical upstream SDK before claiming a field is misattributed |
| 048.md | Fee-bump `tx_signers` drops the inner transaction authorizers | `tx_signers` intentionally holds outer fee-bump envelope signatures per Horizon convention (mirrors `source_account`/`fee_account`/`signatures` split); repository tests explicitly assert outer signatures for fee-bump rows | reviewer | For fee-bump transactions, `tx_signers` follows Horizon's convention of containing the outer fee-bump signatures; inner authorizers are accessible only through the inner transaction sub-object; verify Horizon's API contract before claiming the "wrong" signer set |
| 049.md | `invoke_contract` can report another contract's `contract_code_hash` | PoC partially demonstrated footprint-based hash mismatch but final review rejected it: `contract_code_hash` is intentionally populated from the Soroban footprint rather than authoritatively resolved for the invoked contract; no enforced contract ties it to `details.contract_id` | final-review | A PoC demonstrating that two fields can diverge is insufficient without a documented or code-enforced invariant tying them together; field mismatch may be an interpretation question rather than confirmed data corruption |
| 050.md | Contract-code cost-input counters truncate large Wasm metadata values | XDR source `ContractCodeCostInputs` defines all ten counter fields as `Uint32`; the `uint32(...)` casts are type-preserving, not narrowing | hypothesis | Repeated `uint32(...)` casts are only actionable when the upstream XDR member is wider than `Uint32`; confirm the generated XDR field type before treating a cast-heavy transform as a live narrowing bug |
| 051.md | Claimable-balance `asset_amount` uses the lossy inline float path instead of the shared stroop helper | Exact duplicate of published finding `success/data-transform/016-claimable-balance-inline-float64-rounding`; same mechanism (`float64(amount)/1.0e7` vs `ConvertStroopValueToReal()`), same trigger, same impact | reviewer | Always check `ai-summary/success/{SUBSYSTEM}/` before submitting; the inline float64 bug was already confirmed and published as finding 016 |
| 052.md | Buy-side normalized offer `price` carries raw offer-side units instead of canonical counter/base | PoC passed but was rejected at final review: `DimOffer.Price` is internally consistent with the currently emitted `BaseAmount` and `CounterAmount` (invariant `CounterAmount == BaseAmount × Price` holds); the mismatch only appears against hypothetically corrected amounts from finding 021 — not against actual current output | final-review | A PoC demonstrating that two fields differ is insufficient when the row's own internal invariant holds in the current output; a field is only an independent defect if the invariant breaks on current — not hypothetically corrected — values |
| 053.md | Parallel generalized tx-set ordering produces wrong ledger counts | Could not establish concrete desynchronization between the generalized tx-set envelope-flatten order and `TxResultSet.Results` order; local captive-core reconstruction and upstream `LedgerCloseMeta` SDK helper both use matching processing-order traversals | hypothesis | Parallel-stage metadata alone is insufficient to claim count corruption; a concrete format contract or test fixture demonstrating mismatched ordering is required before the index-zip can be treated as a bug |
| 054+055.md | `tx_fee_meta` (history_transactions / ledger_transaction) drops P23+ fee-refund changes | `tx_fee_meta` has always serialized only `transaction.FeeChanges` (pre-apply fee debit) — never post-apply refund changes; the P23+ test fixture with `PostTxApplyFeeChanges` explicitly asserts the `FeeChanges`-only convention; upstream Horizon's `fee_changes_xdr` uses the same contract | reviewer | `tx_fee_meta` is the pre-apply fee debit blob, not the full fee lifecycle; a hypothesis about missing refund XDR should target the absence of a dedicated `PostTxApplyFeeChanges` blob column, not the pre-existing `tx_fee_meta` field |
| 056.md | Failed fee-bump wrappers can panic `TransformTransaction()` via unconditional `InnerHash()` | The Stellar protocol prevents wrapper-level failures from entering closed ledgers; only `TxFeeBumpInnerSuccess` and `TxFeeBumpInnerFailed` can appear in history archives, both of which carry `InnerResultPair`; upstream Horizon uses the identical unconditional pattern | reviewer | Fee-bump wrapper-level failures are rejected by stellar-core before ledger inclusion; before hypothesizing a panic from a `Must*()` call, verify whether the protocol guarantees the union arm is always populated in archived data |
| 057.md | Liquidity-pool `rounding_slippage` sentinel values (`math.MaxInt64`/`math.MinInt64`) are a transform bug | The workaround is copied verbatim from the upstream `stellar/go` trade processor and represents the current SDK-contract convention, not an ETL-specific divergence | hypothesis | When a suspicious transform branch is copied verbatim from the upstream `stellar/go` processor, treat it as an SDK-contract question first; mirror behavior is only viable if the ETL diverges from or adds extra corruption on top of upstream |
| 058.md | Account `sequence_time` can overflow negative through `int64` narrowing | `SeqTime` tracks timestamp-scale Unix values; reaching `math.MaxInt64` requires a far-future chain state not achievable on realistic ledgers — the trigger is only theoretical | hypothesis | Not every `uint64 → int64` cast is a live integrity bug; for timestamp-backed fields, verify that the domain can realistically reach the dangerous range before treating the narrowing as actionable |

## Meta-Patterns

1. **XDR Field Verification First**: Multiple hypotheses (003, 004, 005) failed because the alleged source XDR field does not exist in the Stellar protocol. Always inspect the XDR struct definition before proposing a path mix-up.

2. **Cross-Check Published Findings (All Subsystems)**: Investigations 007–010, 024a, 025, 026, 027, and 028 all re-investigated already-confirmed and published findings. Always check `ai-summary/success/` across **all subsystems** — not just the target subsystem — before submitting; the same underlying bug may be published under a different classification (e.g., `data-integrity` vs `data-transform` vs `export-pipeline`).

3. **Repo Tests as Design Intent**: When the repository's own unit tests and golden outputs lock in a specific representation (014, 015, 017), that is authoritative evidence of design intent. A PoC demonstrating format inequality is insufficient without a codebase contract requiring the other format.

4. **Discriminator Column Recoverability**: A column that collapses to `0` for inapplicable rows is not unrecoverable data corruption if a sibling discriminator column (e.g., `trade_type`) lets downstream consumers recover the original semantic (016).

5. **SDK Abstraction Layer**: SDK helpers such as `Envelope.SourceAccount()` already handle fee-bump and multi-source disambiguation. Always verify SDK method semantics before assuming that direct XDR field access produces a different result (006).

6. **Intentional Legacy Semantics**: Some exported fields carry historical BigQuery schema conventions that differ from what their name implies (002). Golden output files and checked-in tests are the authoritative source of truth for intended values.

7. **Upstream Model Omissions Are by Design**: When the upstream ingest SDK intentionally omits a field for a certain asset class (013, custom-token `Asset`), the ETL leaving that column empty is correct behavior, not a gap.

8. **Failed-Operation Defaults Match Upstream SDK Convention**: Zero-default outcome amounts for failed path payments (030/032) and native-asset defaults for failed LP operations (031) exactly mirror the canonical `stellar/go` SDK behavior. Before labeling a default value as "fabricated," trace the equivalent upstream SDK processor to confirm whether the pattern is ecosystem-wide convention.

9. **Float64 Schema Design Limit vs. Conversion Bug**: Precision loss through `ConvertStroopValueToReal()` / `big.NewRat().Float64()` is an inherent property of the float64 schema type — the best possible conversion available. This is NOT_VIABLE unless the code either (a) uses an inferior inline conversion when the shared helper was available, or (b) computes an exact string and then unnecessarily round-trips it through `ParseFloat`. Investigations 034–038 all fell into category (c): no better conversion path existed but was unused.

10. **Dead Code Is Not a Better Path**: `transactionOperationWrapper.Details()` is never called anywhere in the codebase (confirmed via grep). Citing dead code as evidence of "a precision-preserving alternative that was not used" does not satisfy the VIABLE criterion. Always verify a cited alternative is reachable before treating it as proof of a missed opportunity.

11. **Soroban Footprint Exhaustiveness Guarantee**: The Soroban protocol requires every transaction to declare its complete footprint via preflight simulation. For `create_contract`, the `ContractInstance` ledger key MUST be in `ReadWrite`; for `upload_wasm`, the `ContractCode` key MUST be in `ReadWrite`. Test fixtures that use generic envelopes with no `SorobanData` produce empty footprint-scanner results, but this does not reflect production behavior (045, 046).

12. **Fee-Bump Field-Split Follows Horizon Convention**: For fee-bump transactions, the ETL mirrors Horizon's `source_account`/`fee_account`/`signatures` split: `account` = inner source, `fee_account` = outer fee-bump source, `tx_signers` = outer fee-bump signatures. Outer fields are intentional overwrites, not bugs (006, 033, 048).

13. **Internal Row Invariant Must Hold on Current Output**: A PoC demonstrating that two fields diverge from a canonical expectation is insufficient when the row's own internal invariant (`CounterAmount == BaseAmount × Price`) still holds on the current output. A field is only an independent defect if the invariant breaks on the data the ETL actually emits, not on a hypothetically corrected version of it (052).

14. **Parquet-go Tag Grammar Is Idiosyncratic**: parquet-go uses `type=MAP, convertedtype=LIST` for `[]string` repeated columns — matching its own documented examples. Duplicate `convertedtype=` keys are parsed last-wins by `StringToTag()`. Neither is a live schema corruption without a demonstrated parser failure (041, 042).

15. **Protocol Guarantees Make Some `Must*()` Calls Safe**: Fee-bump transactions in closed ledgers can only carry `TxFeeBumpInnerSuccess` or `TxFeeBumpInnerFailed`. Wrapper-level failures are rejected before ledger inclusion. The unconditional `InnerHash()` / `MustInnerResultPair()` call is safe by protocol invariant, as confirmed by the identical pattern in upstream Horizon (056).
