PUBLISHED: amendment
ISSUE: #32
URL: https://github.com/amishas157/stellar-etl/issues/32
RELATED_TO: export_ledger_transaction parses --write-parquet and --parquet-output flags but discards the parquet path and always follows the JSON-only path, silently dropping the requested parquet artifact
